// Voting eligibility program

#include<stdio.h>

int main()
{
    int age;
    printf("Enter age: ");
    scanf("%d",&age);
	
    if(age >= 18)
    {
        printf("Eligible for voting");
    }
    return 0;
}


if(a>5); // The semicolon ends the if statement. So "Hello" prints every time.
{
    printf("Hello");
} 
